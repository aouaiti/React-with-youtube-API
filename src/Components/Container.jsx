/* eslint-disable max-len */

import React from "react";
import { Container } from "semantic-ui-react";

const Contained = (props) => <Container>{props.children}</Container>;

export default Contained;
